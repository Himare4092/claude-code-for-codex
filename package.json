{
  "name": "claude-plugin-cc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Use Claude Code from Codex for reviews and delegated tasks",
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run build && npm test",
    "setup": "node plugins/claude/dist/cli.mjs /claude:setup"
  },
  "dependencies": { "@modelcontextprotocol/sdk": "1.30.0", "zod": "4.5.4" },
  "devDependencies": { "esbuild": "0.28.2" }
}
